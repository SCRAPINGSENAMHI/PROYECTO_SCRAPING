# ============================================================
# SENAMHI Dashboard - dependencias Python
#
# NOTA SOBRE GDAL: el paquete "GDAL" NO se lista aqui porque
# debe instalarse con la version exacta del sistema operativo:
#   RUN GDAL_VERSION=$(gdal-config --version) && \
#       pip install "GDAL==${GDAL_VERSION}" -r requirements.txt
# ============================================================

# --- HTTP / Scraping ---
requests==2.31.0
urllib3==2.2.0
certifi==2024.2.2
charset-normalizer==3.3.2
idna==3.6
beautifulsoup4==4.12.3
soupsieve==2.5
lxml==5.1.0
html5lib==1.1
webencodings==0.5.1

# --- Data / Excel ---
pandas==2.2.0
numpy==1.26.4
openpyxl==3.1.2
et-xmlfile==1.1.0
python-dateutil==2.8.2
pytz==2024.1
tzdata==2024.1
six==1.16.0

# --- Geoespacial ---
# GDAL: ver nota arriba (se instala en Dockerfile)
geopandas==0.14.0
fiona==1.9.4
shapely==2.1.0
pyproj==3.6.0
rtree==1.0.1
# pyshp: proporciona el modulo "shapefile" usado en server.py
pyshp==2.3.1

# --- Web (Flask + servidor WSGI) ---
Flask==2.3.3
# Werkzeug pinado: Flask 2.3.x requiere Werkzeug 2.3.x
Werkzeug==2.3.7
# MarkupSafe es dependencia directa de Jinja2/Werkzeug
MarkupSafe==2.1.5
Jinja2==3.1.3
itsdangerous==2.1.2
click==8.1.7
blinker==1.7.0
gunicorn==21.2.0
